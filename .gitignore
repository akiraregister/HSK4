node_modules/
tests/__shots/
worker/.wrangler/
# tests/run.mjs を動かすのに `npm i playwright` が要る。
# このリポジトリはビルドを持たない方針なので、その副産物は追跡しない。
package.json
package-lock.json
worker-paywall/.wrangler/
.wrangler/

# 撮影用に落としてくる書体（14MB）。第三者のバイナリなのでリポジトリには入れない。
# 必要になったら node tests/fetch-fonts.mjs で落とし直す。
tests/fonts/
