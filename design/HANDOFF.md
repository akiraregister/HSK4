# 引き継ぎ — デザイン刷新（ここから再開）

最終更新：2026-09-19　ブランチ：`claude/eloquent-davinci-fi7mjq`

## いまどこまで進んだか

| | 状態 |
|---|---|
| フェーズ0（粗取り4件） | **完了**。`index.html` に着手済み |
| フェーズ1（アイコン一式） | **完了**。`assets/` を書き出し済み |
| フェーズ2（デザイントークン） | **完了**。案Xの見た目になった |
| フェーズ3（下タブ・細いヘッダー） | **完了**。ナビの三重化も解消した |
| フェーズ4（今日画面） | **完了**。確定版のとおりになった |
| フェーズ5（Day学習の4ステップ化） | **完了** |
| フェーズ6（完了画面） | **完了**。連続日数の記録も始めた |
| フェーズ7（設定の整理と購入の復元） | **完了** |

**案A（一画面一動作）はこれで全部終わり。**
そのあと全画面を通して監査し、見つかった3件を直した（下の「8.」）。
次に進むなら**案C**（価格画面・無料の区切り・全498語の開示）。モックは `design/index.html` に
あり、画面ごとの対応表は `design/README.md` の「4. 案C」。
下の「4. 次にやること」の番号はそのまま使える。

---

## 1. 決まったこと

| 項目 | 決定 |
|---|---|
| **デザインの方向** | **案X「辞書・新聞」の洗練後**。明朝（Noto Serif JP）＋宋体（Noto Serif SC）、罫線で組む、カードも角丸も影もゼロ |
| **構造** | **案A**（下タブ化／今日画面は主役1枚／Day学習を4ステップ／完了画面を新設／設定を4グループ） |
| **今日画面** | **確定**。復習・模試は行（リスト）にして右端に罫囲みのボタン。進捗は**90マスの格子**（原稿用紙の升目）＋「12 / 90」を30pxで |
| **アイコン** | **O7「二段の炎」**を設計どおり（生成り地／上＝山吹／下＝柿）。下の「2.」に確定値 |
| **色の方針** | **朱から離れる。**「中国＝赤」の記号を避け、暖色へ。アプリの元ブランド色がアンバー `#F2A227` なので実質そこへ戻る |
| **洗練の8項目** | タイプスケール6段に固定／余白を4pxグリッド／数字を主役／色を1つ減らす／下端に地面を作る／囲いを外す／中文とピンインの対比／影を消して罫を2階層。**どの方向でも効くので確定扱い** |

## 2. アイコンとアクセント色（確認済み・決定）

前回ここが宙に浮いていたが、確認して決まった。

| 項目 | 決定 |
|---|---|
| **アイコン** | **O7 を設計どおり。** 生成り地 `#F6F2E6`／炎の上段＝山吹 `#E5A500`／下段＝柿 `#DC6011`／下の罫＝墨 `#15150F` |
| **UIのアクセント色** | **柿 `#DC6011`。** 今日画面の「復習する」ボタンの罫と文字、90マスの「今日」に使う |

`#E8981F` は**アイコンの色ではなくアクセント色の指定だった**（前回の宙ぶらりんはこの取り違え）。
そのうえでアクセントは、クリーム地に対する明度差が取れる柿 `#DC6011` を選んだ。
結果としてアクセント色はアイコンの下段と同じ色になり、揃っている。

生成り地は「明るい壁紙のホーム画面で輪郭が溶ける」案だが、**それを承知でO7のまま**と決めた。

## 3. モックアップの置き場所

| ファイル | 中身 |
|---|---|
| `design/index.html` | 構造（案A・案C）のモック。「いま／改善案」を切り替えて比較できる |
| `design/directions.html` | 視覚デザインの方向4案（定番） |
| `design/directions-bold.html` | 世界観をゼロから考えた3案（辞書／計器／切符）。**ここで案Xを選んだ** |
| `design/refined.html` | 洗練の前後比較（8項目） |
| `design/x-next.html` | 案Xのアイコン候補6案＋**今日画面の改善（確定版）** |
| `design/icons-flame.html` | 炎アイコン 第1ラウンド（8案） |
| `design/icons-flame2.html` | 第2ラウンド（8案）。小さいサイズでの生存条件が分かった回 |
| `design/icons-flame3.html` | 第3ラウンド（9案）。**朱から離れて暖色へ。ここでO7を選んだ** |
| `design/current/*.png` | 現行アプリの画面（比較用。`index.html` へ data URI で埋め込み済み） |
| `design/README.md` | 分析（問題21件）・3案の比較・画面ごとの対応表・全ラウンドの記録 |

**`design/README.md` を先に読むこと。** 何をなぜ変えるかが全部書いてある。

## 4. 次にやること（実装）

`design/README.md` の「3. 案A — 画面ごとの対応表」と「4. 案C」に、触る関数まで書いてある。

### フェーズ0：粗取り — **完了**（コミット済み）

1. ~~ブックマークタブにDay画面の下ナビが出ている~~ → `render()` の bookmark 分岐を
   `display='flex'` の手前へ移し、分岐の中で下ナビを隠した
2. ~~「スキップして始める」がボタン内で改行する~~ → 文言を「スキップ」に短縮
3. ~~ブックマークでピンインが二重に出ている~~ → 呼び出し側で `pinyin` 引数として渡すよう直し、
   保存済みデータ向けに `fmtBmItem()` でも先頭の拼音を落とすようにした
4. ~~決済まわりの `alert()`~~ → `_toast(msg, 6000)` へ。`window._toast` も公開した

※ 受験予定日の `mm/dd/yyyy` は**撮影環境（Chromium）の表示で、実機の Safari では 年/月/日 になる**。直す必要はない。

※ **クラウド同期の `alert()` 3件は残してある**（`index.html` の「先にログインしてください」
  「クラウドにデータがありません」）。決済まわりではないので今回の範囲から外した。
  同じ扱いに揃えるなら、ついでにトーストへ寄せるとよい。

### フェーズ1〜6（案A本体）
1. ~~**アイコンをSVGで仕上げ、`assets/` のPWAアイコン一式を書き出す**~~ → **完了**。
   `node assets/build-icons.mjs` で `icon.svg` `icon-maskable.svg` `icon-192.png`
   `icon-512.png` `icon-512-maskable.png` `apple-touch-icon-180.png` を1か所から書き出す。
   色と形を変えるときはこのスクリプト冒頭の定数だけ触ること。
   ファイル名は前と同じなので `manifest.json` と `index.html` の `<link>` は**変更不要だった**
   （4つとも既に正しい先を指している）。
   - **残件：アプリ内ヘッダーとLPの炎がまだ旧デザイン**（`index.html` の `.brand-flame`、
     `lp/index.html` の2か所）。顔つきの丸い炎のまま。ヘッダーはフェーズ3で作り直すので
     そこで差し替える。LPは別トラック
   - **残件：`manifest.json` の `background_color` / `theme_color` が `#FCF9F2` のまま。**
     アイコンの地は `#F6F2E6` なので起動スプラッシュにわずかな継ぎ目が出る。
     フェーズ2でトークンを決めるときに揃えること
2. ~~デザイントークンを案Xへ差し替え~~ → **完了**。
   - `:root` を案X（`design/refined.html` の `.edi2`）へ。ただし `--brand` は朱ではなく
     **柿 `#DC6011`**（上の「2.」の決定）
   - 書体：明朝 Noto Serif JP ＋ 宋体 Noto Serif SC。丸ゴシック・角ゴシック・Yomogi は外した。
     **拼音だけは声調記号のため system font のまま**（字間だけ `.12em` に広げた）
   - 角丸：`--r-sm` `--r-md` `--r-lg` `--r-pill` の4段を作って**全部 0**。
     直書きの `border-radius` 69か所をトークンへ寄せた（スピナーの `50%` だけ残した）
   - 影：6系統あったのを `--sh` 1つに集約して `none`。
     影が消えて浮かせられなくなったモーダルだけ罫で囲った
   - 罫2階層：`--hair`（細罫）／`--rule`（太罫）。旧名の `--line` `--stroke` `--bw` は
     この2つを指すようにして呼び出し側は据え置いた。
     **`--stroke` を塗りに使っていた5か所は `--ink` に直した**（枠線と塗りが同じ名前だった）
   - 直書きの色を約100か所トークンへ。カードの白（31か所）は地の色に寄せた
   - `#bgShapes` は `display:none`（案Xは地に色を敷かない）。`renderBgShapes()` は手つかず
   - `manifest.json` と `<meta name="theme-color">` を新しい地の色 `#FCFBF8` に揃えた
   - `CLAUDE.md` の「書体」「色」の節も書き直した（丸ゴシック前提のままだったため）
   - **残件：アプリ内ヘッダーの炎（`.brand-flame`）とLPがまだ旧デザイン。**
     ヘッダーはフェーズ3で作り直すのでそこで差し替える
   - **残件：`button.primary` は墨のベタ塗りのまま。** `design/refined.html` の案X洗練後は
     「罫で囲った透明のボタン」（`--btn:transparent`）。今日画面を作るフェーズ4で判断する
3. ~~上の4タブ → 下タブへ。ヘッダーを細いバーに~~ → **完了**。
   - `.tabbar` を画面下に固定。**`.nav-tab` のクラス名とID（`homeBtn` 等）は据え置いた**ので
     `setupNavTabs()` と `setActiveTab()` はそのまま効く
   - ヘッダーは約300px → 約70px。ブランド（左）と「12 / 90」（右）だけ。
     **ここの「12 / 90」はフェーズ6で連続日数に差し替える予定の枠**
   - ヘッダーの炎を**アイコンと同じO7**に差し替えた（フェーズ1の残件を回収）
   - **ナビの三重化（C1）も同時に解消した。** 下タブを足すと四重になってしまうため、
     ここだけはフェーズ5の項目を前倒しした：
     - Day学習中は**下タブを隠し**、`#bottomNav`（前／完了／次）だけにする
     - 出口はヘッダーの「**✕ 今日へ**」1つ（`.top.day` で切り替え）
     - `.daybar`（前の日／Day選択／次の日）を削除。`initSelect()` と
       `daySelectOptionsHTML()` が呼ばれなくなったので関数ごと落とした。
       **任意のDayへ飛ぶのは今日画面の「全90日」の折りたたみから**
   - `setChrome()` を新設し、下タブ／下バー／ヘッダーの出し分けを1か所に集約。
     以前は各 `render*()` が `bottomNav` を手で出し入れしていた（フェーズ0で直したバグの原因）
   - 広い画面（800px以上）で `.bottomnav` を消す指定を外した。
     下タブに移したので、消すとPC表示でナビが無くなる
   - `tests/nav-test.mjs` を更新（36 → 45項目）。`tests/track-test.mjs` も1か所、
     Day学習中の `#homeBtn` クリックを `#topBack` に直した
   - **残件：設定タブのアイコンが22pxだと歯車より太陽に見える。** モックのまま持ってきた
   - **残件：LPの炎はまだ旧デザイン**（別トラック）
4. ~~今日画面（確定版のとおり）~~ → **完了**。`design/x-next.html` の確定版に合わせた。
   - 主役は `.hero` 1枚だけ。Day番号を40pxで置いた（以前は新規と復習が同じ大きさで並び、
     初日は「まだありません」がDay 1と同じ大きさだった＝分析のB3）
   - 復習・模試は**行**（`.trow`）にして、右端に罫囲みの「復習する」「受ける」を置いた。
     数字は左に残して一覧性を保つ
   - 進捗は**90マスの格子**（`.prog-grid`）。完了＝墨／今日＝柿／これから＝空マス。
     「12 / 90」を30pxで置き、太罫で区切って下端へ寄せた
   - **進捗リング（13%）と週の帯と手書きの波線（`.squiggle`）は廃止。** 90マスが週も月も
     含むので週だけ別に出す必要がない。`.tcard` `.today-actions` `.squiggle` のCSSも消した
   - ボタンは塗りつぶさず**罫で囲う**（`.xbtn`）。フェーズ2で持ち越した
     「`button.primary` が墨のベタ塗りのまま」はこの画面については解消
   - 「あたらしく覚える」の緑をやめて muted に（洗練の4番「色を1つ減らす」）
   - **390×844なら1画面に収まる**ことを実測で確認（12日完了・模試ありの状態で844/844）。
     iPhone SE（667px）ではスクロールする
   - **heroからDayの題（「HSK4 基礎強化」等）を落とした。** 確定版のモックに合わせた。
     縦を1行ぶん詰めるためでもある。戻すなら `renderHome()` の `.hero-m`
   - テスト：`tests/nav-test.mjs` を45→49項目に（`.hero`が1枚、`.trow-rev`、90マス、
     進捗の文字サイズ）。`.tcard` を見ていた3か所と `tests/mock-test.mjs` の
     「腕試し」判定2か所・クリック1か所を新しい構造に直した
5. ~~Day学習を4ステップ化~~ → **完了**。
   - 単語→文法→聞く→解く。**文法や音声が無い日は自動で抜ける**ので、ステップ数は3〜4になる
   - 上に進捗バー（`.dsteps`）、下は「次へ」1つ。最後のステップでだけ「完了」に変わる
   - `#bottomPrev` / `#bottomNext` は**Dayの前後からステップの前後へ**役目を変えた。
     Dayをまたぐ移動は今日画面の「全90日」から（1日1Dayが商品の形、という決定に沿う）
   - **単語は1語ずつめくる**（`design/README.md`「まだ決まっていないこと」1番の決着。
     利用者が「1語ずつめくる」を選択）。カードのタップ／「次へ」／下の点のどれでも進む
   - **ステップ移動は描き直さず表示だけ切り替える**（`.dstep.on`）。描き直すとミニテストと
     リスニングの途中経過が飛ぶため
   - **ついでに直したこと：** `toggleBookmark()` `setLevel()` `toggleComplete()` が
     `render()` を呼んでいたので、学習の途中で★・難易度・完了を押すとミニテストと
     リスニングが最初からやり直しになっていた（4ステップ化で表面化した既存バグ）。
     いまは `data-bm` / `data-lv` を手がかりにその場の要素だけ更新する
   - テスト：`tests/nav-test.mjs` を49 → 65項目に。ステップの進み戻り、1語ずつ出ること、
     最後のステップでだけ完了が出ること、★と難易度で位置が飛ばないことを見る。
     `tests/track-test.mjs` は完了ボタンまで「次へ」を押し切るようにした
   - **残件：Day画面の見出しカード（「Day 13 | HSK4 基礎強化」と今日の内容）を外した。**
     Day番号はヘッダーに出ているため。題を出したいならステップラベルの脇が候補
6. ~~完了画面を新設（連続日数・その日の数字3行）~~ → **完了**。
   - `currentView='done'` を追加。完了ボタンを押すとここへ送られる
   - ✓・「Day N 完了」・連続日数の一言・その日の数字3行（単語／文法／ミニテスト）・
     「復習を続ける（N問）」「今日はここまで」の2択
   - **次のDayへ進むボタンは置かない**（1日1Dayが商品の形、という決定どおり）。
     まとめて進めたい人は今日画面の「全90日」から入る
   - **連続日数の記録を始めた。** `state.completedAt`（Day番号 → 完了時刻ms）を追加し、
     `normalizeStateForCloud()` にも足した（列挙式なので書き忘れると同期で消える）
   - **記録を始める前の完了日は復元できない**ので、既存の完了分は連続日数に入らない。
     「今日から記録して連続日数も出す」（案A）で進める判断。利用者がまだ少ないため
   - ヘッダー右の「12 / 90」は、連続日数が1日以上あるときだけ「N日連続」に差し替わる。
     0日のときは今までどおり「12 / 90」（枠の予定どおり回収した）
   - テスト：`tests/nav-test.mjs` を65 → 74項目に。完了画面が出ること、数字が3行、
     連続日数の文言、下バーが出ないこと、次のDayボタンが無いこと、
     **`completedAt` が `normalizeStateForCloud()` に載っていること**（同期落ちの見張り）
7. ~~設定を4グループに整理＋「購入を復元」~~ → **完了**。
   - アカウント／学習／表示／その他の4グループ。境目は太罫、見出しは字間を広げた小さな字
   - **アカウントだけは静的な markup 側にある**（同期バーは Firebase 側が ID を直接引くので
     動かせない）。`renderSettings()` は `#setAccountExtra` へ「購入を復元」だけを足し、
     残り3グループを `#settingsBody` に描く
   - **「購入を復元」を常設した**（分析のA6）。以前はDay8以降の購入画面の中にしか無く、
     機種変・再インストールのときに辿り着けなかった。失敗時も `alert()` だった。
     いまは `restorePurchase()` がログイン状態を見て、結果をトーストで返す
   - 文字サイズは「表示」グループへ移した（グループの順番を揃えるため、静的な markup から
     描画側へ移動。`applyFontScale()` は `render()` より後に描かれるので、`on` は生成時に付ける）
   - **選択肢は折り返すチップの群れをやめ、罫で仕切った1行のセグメント（`.seg`）に畳んだ**
   - パネル全体を囲う枠を外した。グループの境目を太罫で作るので入れ子の囲いが要らない
   - テスト：`tests/nav-test.mjs` を78 → 83項目に。4グループあること、並び順、
     セグメントに畳まれていること、「購入を復元」が**アカウントのグループに**あること

## 4.5 実装後の監査で直したこと

案Aを全部終えたあと、全画面をブラウザで操作してエラーと崩れを探した結果。
**JSエラーと横方向のはみ出しはゼロ**、固定バーに隠れる操作要素も無し。
そのうえで見つかった3件を直した。

1. **初回起動の級診断に下タブが出ていて、しかも「設定」が選択状態だった**（分析のB2）。
   級診断は普段は設定から入る画面なので `setActiveTab()` が settingsTab を選ぶが、
   初回起動では意味が通らない。`setChrome()` に `hasOnboarded()` の判定を足し、
   初回のオンボーディングだけ全画面にした。`tests/onboard-test.mjs` に2項目追加
2. **完走時の「90日ぶん完走」が40pxで大きすぎた。** `.hero-day .n` はDay番号用の
   大きさなので、文が入るときは `.txt` で26pxに落とす
3. **`.card` の外枠を外した**（分析のC4「カードの入れ子」）。案Xはカードを地から
   浮かせず罫だけで組む。今日・Day・完了・設定は作り直し済みで枠が無いのに、
   復習・模試・ブックマーク・単語・級診断だけ外枠が残って入れ子になっていた。
   **横幅が広がったので、ブックマークの並び替えチップの折り返しも同時に直った**

あわせて、無くなった `completeTab` を参照する死んだコードを `updateCompleteButtons()`
から落とした。

### 監査のときに引っかかった誤検知（次に調べる人向け）

「固定の下バーが最後のボタンを覆っている」と出たが、**誤検知**だった。
閉じた `<details>`（全90日から選ぶ）の中身は `getBoundingClientRect()` が
実寸を返すため、描かれていないボタンを拾っていた。
`element.checkVisibility({contentVisibilityAuto:true})` で除くと消える。

## 4.7 案C（価格画面・無料の区切り）— 途中まで実装済み

### 済んだもの

- **価格画面**（`lockedDayHTML()` を全面書き換え）。以前は「未購入」の札・クーポン欄・
  購入ボタンだけで、**値段も中身も一度も出ないまま決済画面へ飛ばしていた**（A1）
  - 無料と全90日の比較表6行。買い切りであることを価格の真横に
  - **クーポン欄は `<details>` で畳んだ**（A2。常時出ていると全員にコードを探しに行かせる）
  - 「購入を復元」と特商法へのリンクを細字で
  - **未購入のDayでは学習用の下バー（戻る／完了）を出さない。**
    出したままだと未購入のDayを「完了」にできてしまう（実装中に踏んだ）
- **無料の区切り**（`freeEndHTML()`）。Day 7 の完了画面に、やり切ったことを認めてから
  Day 8 以降の量を示し、価格画面へ渡す一節を足した（A4）
- **単語タブに全498語を出して未購入分に🔒**（A5）。
  `build-content.mjs` が `LOCKED_VOCAB`（**中文とDay番号だけ**）を `index.html` に残し、
  単語タブがそれを足して498語を並べる。未購入の行は拼音も和訳も持たない＝
  教材の厚みは見せるが中身は渡さない。タップすると価格画面へ行く
  - **ここでスクリプトを壊した。** ブロックを前から置換したせいで後ろのブロックの
    オフセットがずれ、`index.html` の別の場所へ463語を書き込んだ。作業用コピーで試して
    いたので本体は無事。いまは降順に置換し、**書き出す前に読み直して検算する**
    （合わなければ書かずに終了コード1。わざとバグを再現して、止まることを確認済み）
  - 切り出し→書き戻しの往復で `index.html` がバイト単位で元に戻ることも確認した
- **paywallのプレビュー**（下記）

### まだのもの

- **ログインを購入の前提にしない**（A3。先に決済し、あとで紐付ける）。
  これは**Cloudflare Worker（`worker-paywall`）とStripeの流れを変える**話で、
  アプリ側だけでは閉じない。配置済み・稼働中のWorkerを触るので、
  手順と切り戻しを決めてから着手すること

### paywallのプレビュー（販売前の確認用）

全90日が `index.html` に入っているあいだ、価格画面と無料の区切りは**一度も出ない**。
内容を切り出さずにそれらの画面だけ見られるようにした。

- 見る：URLに `?paywall=1`（設定 → その他 → 「paywallのプレビュー」でも切替）
- 戻す：URLに `?paywall=0`、またはトグルを「切」に
- 判定は `paywallPreview()` → `lessonAvailable()` と `lesson()` の2か所だけ。
  学習データには触らないので、切ればすぐ全90日に戻る
- 販売開始時に `build-content.mjs` で切り出せば、このスイッチに関係なく本来の判定が効く

## 5. 実装するときの落とし穴

- 画面遷移は必ず `goView()` を通す（`currentView` 1つで持つ設計）。新しい画面（完了・価格）も値を足して同じ経路に乗せる
- `LESSONS.length` ではなく `TOTAL_DAYS`（固定90）を使う。未購入だとDay8-90は存在しない
- **`state` に新しいフィールドを足したら `normalizeStateForCloud()` にも足すこと。**
  忘れるとFirebase同期で消える（enumerateしている）
- ~~ストリークには完了日時が要る~~ → 対応済み（フェーズ6）。`state.completedAt` を追加した。
  **記録を始める前の完了日は復元できない**ので、連続日数は実質「記録を入れた日からの数」
- ~~下タブ化は `tests/nav-test.mjs` が落ちる~~ → 対応済み（フェーズ3）。
  **Day学習中は下タブが隠れるので、テストから `#homeBtn` を押せない。** 戻るときは `#topBack`
- 変更したら必ず `node tests/run.mjs`（6スイート）を通す
- **Claude Code on the web（コンテナ）で画面を撮るとき**は2つ引っかかる。手元のMacでは起きない
  - Playwright の Chromium は `HTTPS_PROXY` を見ないので、`chromium.launch({proxy:{...}})`
    で渡す。そのとき `bypass:'127.0.0.1,localhost'` を付けないとローカルの静的サーバーへの
    リクエストまでプロキシに食われる
  - それでも Google Fonts はプロキシのCAを Chromium が信用せず読み込めない。
    **撮れた画像の書体はフォールバックなので、明朝の出来はコンテナでは確認できない**
- 書体：丸ゴシックは使わない方向になった（案Xは明朝）。`--font-brand` `--font-display` の扱いを整理すること

## 6. いまは触らない（別トラック）

販売開始に関わるもの。`design/README.md` とルートの `CLAUDE.md` に詳細がある。

- リポジトリのPrivate化／Cloudflare Pagesへの移行（中国からの到達性・独自ドメインも同時に解決する）
- 特定商取引法の事業者情報5項目
- Stripeの本番モード切り替え
- ファネル計測（`track()` の送り先が未設定で、データが端末から出ていない）
- 案Cの残り：単語タブの全498語開示（build-content.mjs の変更が要る）と、
  ログインを購入の前提にしない話（Workerを触る）。上の「4.7」参照
- App Store配信時のIAPと引き換えコードの扱い

## 7. 販促素材（`marketing/`）について

App Store用スクリーンショット7枚とLPモックアップが `marketing/` にある。
**デザイン刷新後は端末画面を撮り直すこと。** 版下（帯・見出し）はそのまま使えるので、
`node marketing/capture-app.mjs` → `node marketing/capture-sheets.mjs` の再実行が中心。

Linuxで撮る場合は日本語フォントの設定が要る（`marketing/README.md` の「5. 作り直し手順」）。
